require(["hooves/hooves"], (function (module47c9484436)  {
  return (function (arguments)    {
    // ("hooves/hooves" :all)
    ;
    var windowAlias100040 = window;
    var setModuleObject = (function (valueG100118)      {
      windowAlias100040 = valueG100118;
      });
    return (function (arguments)      {
      // _current-module
      // macro - no dynamic value.
      ;
      // define+
      // macro - no dynamic value.
      ;
      // define-macro+
      // macro - no dynamic value.
      ;
      // define-pattern+
      // macro - no dynamic value.
      ;
      // ("hooves/hooves" :all)
      ;
      windowAlias100040.test1 = (function (x)        {
        return console.log(x);
        });
      windowAlias100040.value = 10;
      return windowAlias100040.test1((("Hello inside module.")+("cat")+("dog")));
      }).call(this, (((((typeof arguments))===("undefined")))?(undefined) : (arguments)));
    ;
    }).call(this, (((((typeof arguments))===("undefined")))?(undefined) : (arguments)));
  }));
console.log("After");
