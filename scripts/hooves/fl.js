define(["hooves/hooves"], (function (module47c9484436)  {
  var currentModule106421 = {    
    
  };
  var setModuleObject = (function (value)    {
    currentModule106421 = value;
    });
  (function (arguments)    {
    // _current-module
    // macro - no dynamic value.
    ;
    // define+
    // macro - no dynamic value.
    ;
    // define-macro+
    // macro - no dynamic value.
    ;
    // define-pattern+
    // macro - no dynamic value.
    ;
    return // ("hooves/hooves" :all)
    ;
    }).call(this, (((((typeof arguments))===("undefined")))?(undefined) : (arguments)));
  return currentModule106421;
  }))