define(["hooves/hooves", "hooves/operator-functions"], (function (moduleminus4745e9d952, moduleminus58c4bce870)  {
  var currentModuleminus14635 = {    
    
  };
  var setModuleObject = (function (value)    {
    currentModuleminus14635 = value;
    });
  (function (arguments)    {
    // define+
    // macro - no dynamic value.
    ;
    // define-macro+
    // macro - no dynamic value.
    ;
    // define-pattern+
    // macro - no dynamic value.
    ;
    // ("hooves/hooves" :all)
    ;
    // ("hooves/operator-functions" :all)
    ;
    currentModuleminus14635.Symbol = (function (name)      {
      this.name = name;
      return this;
      });
    currentModuleminus14635.Symbol.prototype.toString = (function ()      {
      return moduleminus58c4bce870.plus("", this.name);
      });
    currentModuleminus14635.intern = (function (name)      {
      return (new (currentModuleminus14635.Symbol)(name));
      });
    currentModuleminus14635.symbol = (function (name)      {
      return (new (currentModuleminus14635.Symbol)(name));
      });
    return // symbol
    // pattern - no dynamic value.
    ;
    }).call(this, (((((typeof arguments))===("undefined")))?(undefined) : (arguments)));
  return currentModuleminus14635;
  }))